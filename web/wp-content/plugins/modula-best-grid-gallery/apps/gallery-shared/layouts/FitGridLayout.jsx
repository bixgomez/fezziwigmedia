/**
 * Modula Gallery — Fit grid layout (equal-sized cells, full image / object-fit contain).
 *
 * @package
 */

import EqualCellGridLayout from './EqualCellGridLayout';

export default function FitGridLayout() {
	return <EqualCellGridLayout variant="fit" />;
}
