/**
 * Modula Gallery — Uniform grid layout (equal-sized cells, object-fit cover).
 *
 * @package
 */

import EqualCellGridLayout from './EqualCellGridLayout';

export default function UniformGridLayout() {
	return <EqualCellGridLayout variant="uniform" />;
}
