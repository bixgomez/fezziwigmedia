/**
 * Takeover editor is desktop-only. Keep in sync with
 * `assets/css/admin/gallery-takeover-admin.css` (`max-width: 1024px`).
 */
export const DESKTOP_EDITOR_MIN_WIDTH_PX = 1025;

export const DESKTOP_EDITOR_MEDIA_QUERY = `(min-width: ${DESKTOP_EDITOR_MIN_WIDTH_PX}px)`;
