/**
 * Shared gallery UI: layouts, Redux store, bootstrap helpers.
 *
 * Public entry points (do not import deep paths from apps):
 * - `gallery-shared/runtime` — visitor gallery bootstrap
 * - `gallery-shared/preview` — settings-editor preview bridge
 *
 * @package
 */
export * from './runtime';
